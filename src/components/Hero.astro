<!-- ============================================ --><!--                    Hero                      --><!-- ============================================ -->
<section id='hero-1426'>
  <div class='cs-container'>
    <div class='cs-content'>
      <span class='cs-topper'>Logistică de Înaltă Performanță</span>
      <h1 class='cs-title'>
        Soluții complete de fulfillment pentru afacerea ta.
      </h1>
      <p class='cs-text'>
        Oferim servicii profesionale de depozitare, prelucrare comenzi și
        livrări rapide, pentru a-ți optimiza lanțul de aprovizionare și crește
        satisfacția clienților.
      </p>
      <div class='cs-button-group'>
        <a href='' class='cs-button-solid cs-button1'>Să începem</a>
        <a href='' class='cs-button-solid cs-button2'>Află mai multe</a>
      </div>
    </div>
  </div>

  <!-- Background Image -->
  <picture class='cs-background'>
    <source
      media='(max-width: 600px)'
      srcset='/assets/images/hero-mobile.jpg'
    />
    <source media='(min-width: 601px)' srcset='/assets/images/hero7.jpg' />
    <img
      loading='lazy'
      decoding='async'
      src='/assets/images/hero7.jpg'
      alt='field'
      width='1920'
      height='1200'
      aria-hidden='true'
    />
  </picture>
</section>

<style lang='less'>
  /*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

  /* Mobile - 360px */
  @media only screen and (min-width: 0rem) {
    #hero-1426 {
      /* Centers button */
      text-align: center;
      /* 200px - 300px - leaving extra space for the navigation */
      padding: clamp(12.5rem, 25.95vw, 18.75em) (16/16rem);
      /* prevents the topper line from causing an overflow */
      overflow: hidden;
      position: relative;
      z-index: 1;
      min-height: 100vh;
      /* Added for proper height calculation for vertical centering */
      display: flex;
      align-items: center;

      .cs-background {
        width: 100%;
        height: 100%;
        display: block;
        position: absolute;
        top: 0;
        left: 0;
        z-index: -2;

        &:before {
          /* Overlay */
          content: '';
          width: 100%;
          height: 100%;
          background: #000;
          opacity: 0.48;
          position: absolute;
          display: block;
          top: 0;
          left: 0;
          z-index: 1;
          /* prevents the cursor from interacting with it */
          pointer-events: none;
        }

        img {
          position: absolute;
          top: 0;
          left: 0;
          height: 100%;
          width: 100%;
          object-fit: cover;
        }
      }

      .cs-container {
        width: 100%;
        max-width: (1280/16rem);
        margin: auto;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
        gap: (48/16rem);
      }

      .cs-content {
        width: 100%;
        max-width: (630/16rem);
        /* Added for vertical centering on mobile only */
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 100%;
      }

      .cs-topper {
        /* 13px - 16px */
        font-size: clamp(0.8125rem, 1.5vw, 1rem);
        line-height: 1.2em;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-align: center;
        text-transform: uppercase;
        margin-bottom: (16/16rem);
        color: var(--bodyTextColorGray);
        text-shadow: 0px 1px 3px rgba(0, 0, 0, 0.6);
        display: inline-block;
        position: relative;
      }

      .cs-title {
        /* 39px - 61px */
        font-size: clamp(2.4375rem, 6.4vw, 3.8125rem);
        font-weight: 700;
        line-height: 1.2em;
        text-align: center;
        max-width: (829/16rem);
        color: #fff;
        text-shadow: 0px 2px 4px rgba(0, 0, 0, 0.7);
        position: relative;
      }

      .cs-text {
        /* 16px - 20px */
        font-size: clamp(1rem, 1.95vw, 1.25rem);
        line-height: 1.5em;
        text-align: center;
        width: 100%;
        /* 464px - 622px */
        max-width: clamp(29rem, 60vw, 38.785rem);
        margin: 0 auto 0;
        margin-bottom: (32/16rem);
        color: var(--bodyTextColorGray);
        text-shadow: 0px 1px 2px rgba(0, 0, 0, 0.5);
        display: none;
      }

      .cs-button-group {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-wrap: wrap;
        gap: (8/16rem);
      }

      .cs-button-solid {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 3rem;
        padding: 0 1.5rem;
      }

      .cs-button2 {
        background-color: transparent;
        border: 2px solid var(--primary);
        color: var(--bodyTextColorWhite);

        &:hover {
          color: var(--bodyTextColorGray);
          border-color: var(--secondary);
        }

        &:before {
          background-color: transparent;
        }
      }
    }
  }
  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    #hero-1426 {
      /* Reset the flex display for larger screens */
      display: block;

      .cs-content {
        /* Reset vertical centering for larger screens */
        display: block;
        height: auto;
      }

      .cs-text {
        display: block;
      }
    }
  }
</style>
